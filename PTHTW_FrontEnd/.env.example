# Copy to ".env" or ".env.local" for local development (Vite loads these automatically).
# Do not commit files that contain secrets.

# Spring Boot API root URL (no trailing slash). Must match Phase 1 CORS allowed origins.
VITE_API_BASE_URL=http://localhost:8080
