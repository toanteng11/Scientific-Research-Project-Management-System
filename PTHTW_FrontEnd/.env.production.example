# Template for production builds. Leave empty when frontend and backend are
# deployed together on Vercel using the root vercel.json /api service rewrite.
# Set this only when the backend is hosted on a separate domain.

VITE_API_BASE_URL=
